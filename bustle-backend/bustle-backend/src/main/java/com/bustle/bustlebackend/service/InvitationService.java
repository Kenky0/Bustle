package com.bustle.bustlebackend.service;

import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;

@Slf4j
@Service
public class InvitationService { // make edits to this later because might have to make 3 repositories;

//    @Autowired
//    private InvitationRepository invitationRepository;
//
//    public boolean createNewInvitation(Invitation invitation){
//        boolean isSaved = false;
//        Roles invitation = InvitationRepository.getInvitationById(BustleConstants.OPEN);
//        person.setRoles(role);
//        person.setPwd(passwordEncoder.encode(person.getPwd()));
//        person = personRepository.save(person);
//        if (null != person && person.getPersonId() > 0)
//        {
//            isSaved = true;
//        }
//        return isSaved;
//    }
}
