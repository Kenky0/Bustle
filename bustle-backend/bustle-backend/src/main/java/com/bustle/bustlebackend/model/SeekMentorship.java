package com.bustle.bustlebackend.model;

public class SeekMentorship {

//    private long requestId = generateRequestId();
//    private String message;
//    private String status; // use constants;
//
//    // create a many to many database implemtation of invitation to person;
////    @ManyToOne(fetch = FetchType.LAZY, optional = true)
////    @JoinColumn(name = "invitation_id", referencedColumnName = "invitationId", nullable = true)
////    private ArrayList<Invitation> = new ArrayList<>();
//    @Override
//    public long generateRequestId() {
//        long requestId = (long) ((Math.random() * 50000000) + 200000000);
//        return requestId;
//    }

}